name: ai-composiso - Build and Push Docker Image
run-name: ${{ github.actor }} - ai-composiso - Build and Push Docker Image 🚀
on: [push]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "16"

      - name: Install dependencies
        run: |
          cd ai-web-demo
          npm Install

      - name: Build frontend
        run: |
          cd ai-web-demo
          npm run Build

      - name: Build Docker Image
        run: |
          docker build -t ghcr.io/${{ github.repository }}/ai-composiso-frontend:${{ github.sha }} -f Dockerfile .

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Docker Image to GitHub Container Registry
        run: |
          docker push ghcr.io/${{ github.repository }}/ai-composiso-frontend:${{ github.sha }}
